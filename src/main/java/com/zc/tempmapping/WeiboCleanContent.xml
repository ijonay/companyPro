<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zc.tempdao.WeiboCleanContentDao">
    <resultMap id="BaseResultMap" type="com.zc.tempbean.WeiboCleanContent">
        <id column="weibo_id" jdbcType="INTEGER" property="weibo_id" />
        <result column="weibo_content_cleansed" jdbcType="VARCHAR"
            property="weibo_content_cleansed" />
        <result column="comment_content_cleansed" jdbcType="VARCHAR"
            property="comment_content_cleansed" />
    </resultMap>
    <sql id="uniqueQuery">weibo_id =#{weibo_id,jdbcType=INTEGER}</sql>
    <sql id="tableName">WeiboCleanContent</sql>
    <sql id="Base_Column_List">
        weibo_id,
        weibo_content_cleansed,comment_content_cleansed
    </sql> 
    <select id="getItemCount" resultType="java.lang.Integer">
        select count(1) from 
        <include refid="tableName"></include>
        where 
        <include refid="uniqueQuery"></include>
    </select>
    <select id="getList" resultMap="BaseResultMap">
        Select * from
        <include refid="tableName"></include>
        where <include refid="uniqueQuery"></include>
        order by weibo_id
        offset(#{pageSize,jdbcType=INTEGER}*(#{currentPage,jdbcType=INTEGER}-1))
        row
        fetch next #{pageSize,jdbcType=INTEGER} rows only
    </select>

</mapper>