<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zc.tempdao.TopicInfoDao">
    <resultMap id="BaseResultMap" type="com.zc.tempbean.TopicInfo">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="title" jdbcType="VARCHAR"
            property="title" />
    </resultMap>
    <sql id="uniqueQuery">id =#{id,jdbcType=INTEGER}</sql>
    <sql id="tableName">TopicInfo</sql>
    <sql id="Base_Column_List">
        id,title
    </sql>
    <sql id="checkColumnNull">
        <if test="id != null">
            id,
        </if>
        <if test="title != null">
            title,
        </if>
        <if test="coordinate != null">
            coordinate
        </if>
    </sql>
    <sql id="checkPropertyNull">
        <if test="id != null">
            #{id,jdbcType=INTEGER},
        </if>
        <if test="title != null">
            #{title,jdbcType=VARCHAR},
        </if>
        <if test="coordinate != null">
            #{coordinate,jdbcType=VARCHAR}
        </if>
    </sql>
    <select id="getItemCount" resultType="java.lang.Integer">
        select count(1) from 
        <include refid="tableName"></include>
    </select>
    <select id="getList" resultMap="BaseResultMap">
        Select * from 
        <include refid="tableName"></include>
        order by id
        offset(#{pageSize,jdbcType=INTEGER}*(#{currentPage,jdbcType=INTEGER}-1))
        row
        fetch next #{pageSize,jdbcType=INTEGER} rows only
    </select>

</mapper>